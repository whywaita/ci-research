machine:
  timezone:
    Asia/Tokyo

dependencies:  
  override:
    - curl tonkatsu.info

test:
  override:
    - ./spec.sh
    - whois $(curl tonkatsu.info)
    - cat /proc/cpuinfo
    - free -h
    - cat /proc/meminfo
