box: phusion/passenger-ruby22

build:
  steps:
    - script:
        name: install commands
        code: |
          sudo apt-get update
          sudo apt-get install -y curl whois

    - script:
        name: check ci env
        code: |
          curl tonkatsu.info
          whois $(curl tonkatsu.info)
